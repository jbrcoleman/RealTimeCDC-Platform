apiVersion: batch/v1
kind: Job
metadata:
  name: flink-sales-aggregations
  namespace: flink
spec:
  template:
    metadata:
      labels:
        app: flink
        job: sales-aggregations
    spec:
      serviceAccountName: flink-jobmanager
      restartPolicy: OnFailure
      containers:
        - name: flink-job
          image: 296592524620.dkr.ecr.us-east-1.amazonaws.com/flink-sales-aggregations:latest
          command:
            - /opt/flink/bin/flink
            - run
            - --jobmanager
            - flink-jobmanager:8081
            - --python
            - /opt/flink/usrlib/job/job.py
          env:
            - name: AWS_REGION
              value: "us-east-1"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "cdc-platform-kafka-bootstrap.kafka.svc.cluster.local:9092"
            - name: TABLE_PREFIX
              value: "cdc-platform"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
